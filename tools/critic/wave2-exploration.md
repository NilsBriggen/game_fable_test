# Wave 2 — exploration — critic score (round 1/3)

Reference: Skyrim (discovery loop)
Harness run: `schwyz-village-dusk` 2026-09-02T20:40:14Z; `sarnen-night-rain` PENDING; `morgarten-winter` PENDING; `gotthard-schoellenen` PENDING — each run alone on the harness lock, renderer `Google Inc. (Google) / ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)` (SOFTWARE — frame times are an upper bound). Code reviewed at HEAD `81b6069` (`STATUS: exploration built`); `src/exploration/**`, `src/content/pois.ts`, `src/content/npcs.ts` read in full.

Evidence read:
- `npx vitest run src/exploration 2>&1 | tail -6` → ` Test Files  5 passed (5)` / `      Tests  52 passed (52)` / `   Duration  1.53s`.
- `npx tsc --noEmit 2>&1 | grep -E 'src/(exploration|content/(pois|npcs))'` → no output (clean). `node tools/check-imports.mjs` → `imports ok`. (`tools/check-content.mjs` named in ARCHITECTURE §3.3 does not exist — `MODULE_NOT_FOUND`.)
- Harness: `PASS schwyz-village-dusk  calls=470 tris=1766549 p95=2434.6ms max=3179.3ms heap=89MB err=0 warn=0`, report.json `errors: [], warnings: [], entities: 518, state: "explore"`, content `pois: 94, npcs: 98`. Builder's earlier `altdorf-square-noon`: `calls=638 tris=1850110 heap=73MB err=0 warn=0`.
- PNGs read: `tools/harness/out/altdorf-square-noon.png` (19:47), `tools/harness/out/ruetli-dawn.png` (20:17), `tools/harness/out/critic-exploration/schwyz-village-dusk/schwyz-village-dusk.png`. The integrator's `ui/dialogue-gessler-hat` and `ui/menu-map` captures did not exist at the time of writing (only `ui/title`, `ui/menu-inventory`).
- 32 probe tests of my own, outside `src/` (`tools/critic/probes/exploration/*.test.ts`, run with `npx vitest run --config tools/critic/probes/exploration/vitest.config.ts` → `Tests 32 passed`): an itemised content audit, `NpcSystem` driven with a mocked party/world service against the real content, `PoiSystem` with the real content, `generateLayout` on a flat probe and on the **real** 2048×2176 `buildHeightGrid(1291)` (sampling copied from `TerrainManager`), and a source probe of `fastTravel`. Every number below comes from those runs, not from the builder's report.

## Score: 7/10   (pass bar 8)  → FAIL (round 1/3)

The loop's skeleton is genuinely there and works on evidence: 94 POIs at exactly the gazetteer coordinates (deviation 0 m), proximity discovery that fires once and toasts, region-entered toasts, a compass with markers, fast travel from the map with fade + clock advance, `[E] Talk to …` prompts in both village screenshots, a populated Altdorf and Schwyz (crowd around the well, church, houses, hat pole spot on `settlement` surface), a 300 m freeze whose analytic re-entry lands within 1 m of the stepped position, NPCs that refuse to wade, settlements merged to 470–638 draw calls, zero console errors. What keeps it below 8 is that several of the things the rubric names are hollow when probed: three of the ten invented POIs are 4–20 m **under the lake** and get no geometry; Zug (a mandated town) collapses into a heap of 53 interpenetrating stone houses because its footprint is water on 7 of 8 sides; 98 crowd entities (23 % of the world) have a talk prompt that opens nothing; recruited companions are destroyed by `populate()` at every chapter time-skip; boats are decoration, not `travel` interactables (§5.2); the service's `fastTravel` accepts undiscovered ids; and "NPC life" is 8 of 98 named NPCs walking — everyone else, including the whole crowd, stands on its spawn jitter 24 h a day and sleeps in the square at 23:00.

## What the evidence shows (bullets, cite screenshot or number)

Content (probe `content-audit.test.ts`):
- All 30 mandated LORE §4 ids present with the kind I read from §4 (`kind mismatches: none`), `max coordinate deviation from gazetteer: 0 m`, every region matches `PLACE_REGION_ID`, every def has `historical` + `note` + `description`. `POIs total=94 gazetteer-backed=83 invented=10`; discover radii 20–220 m by kind.
- All 12 LORE §5 H/L ids and 7 invented core ids present; chapters exactly as LORE §1/§5 for Gessler (ch1), Landenberg (ch1), Tell (ch1, `role: 'companion'`), Leopold (ch2), Hünenberg (ch2), Winterthur (ch2), the three Rütli men + Attinghausen (all). Equipment ids used by `npcs.ts`: `armbrust, bascinet, bolzen, buckler, coat-of-plates, eisenhut, gambeson, halbarte, heater-shield, hobnailed-boots, lance, leather-cap, mail-shirt, messer, schweizerdolch, schwert, sling, sling-stones, spiess, staff, wooden-shoes` — all LORE §7 / §10. Banned-word grep on NPC descriptions: none. POI descriptions: `poi.schwyz`, `poi.stans` use "canton" (UI text — LORE §7 permits it in UI, bans it only in NPC speech; not counted).
- 79 minor NPCs: no Schiller-only characters; given names are Alemannic (LORE list + Jakob, Toni, Sepp, Niklaus, Balz, Uli, Margret, Gion, Melchior, Barbara, Ita, Rudolf, Anton, Heinrich, Eberhard — all period-plausible); family names are real Innerschweiz families (Kälin, Betschart, Camenzind, Bissig, Businger, Durrer, Omlin, Niederberger, Zurfluh, Infanger, Truttmann…). LORE §8 wants commoners as "given + of <village> / trade-name"; hereditary surnames in 1291 are a soft stretch but the player's own LORE §5 name list does the same, so accepted.
- Generic crowd from `population` tables: **314** unnamed (peasant 127, herder 34, fisher 27, child 21, woman-peasant 18, monk 16, habsburg-footman 15, merchant 12, boatman 11, …) across 73 settlements; 35 distinct home settlements for named NPCs.

Code (probes `npc-system.test.ts`, `layout-poi.test.ts`, `terrain-siting.test.ts`, plus reading):
- Player controller `player.ts:18-24`: walk 1.8 / jog 4.0 / sprint 6.5 / swim 1.2 m/s, `MAX_SLOPE_RAD` 40°, jump 1.2 m, gravity 22, sprint fatigue 6/s (drops to jog at 100), swim fatigue 3/s — per §1/§5.2. Per-axis slope rejection (`tryMove`) so diagonals slide.
- Camera `camera.ts:14-16`: `DEFAULT_DISTANCE = 6`, pitch clamp, 12-sample ray-march `collisionAdjust` against `heightAt`+0.35, damped follow; free/combat/cutscene modes.
- Discovery: crossing the Rütli fires `poi-discovered` exactly once (`['poi.ruetli', 'poi.treib']` — Treib's 70 m disc overlaps the Rütli's 100 m disc, so arriving at the Rütli also "discovers" the ferry landing 144 m away; 6 such overlapping pairs). `nearestPoi` at the Rütli → `poi.ruetli`, at Altdorf → `poi.altdorf`. Toast `Discovered: <name>`; region toast `Entering <region>`; `region-entered` event.
- Fast travel `index.ts:181-194`: fade black 350 ms → teleport → `streamAround(800)` → `clock.advanceHours(dist/4/3600)` → fade clear → `fast-travel` event. **No check of `isDiscovered` or `PoiDef.fastTravel`** (source probe prints the body; `gated: false`). The map UI (`ui/menus.ts:432-437`) only renders discovered markers and only wires `onclick` when `poi.fastTravel`, so the gate exists in the UI alone.
- Schedules: `resolveSchedule` (last entry ≤ hour, wraps midnight) and `analyticPosition` (+ per-entry offset) are correct and tested. Stauffacher frozen 08:00→14:00 then re-entered lands at `(748.0, −1382.6)`; the same NPC simulated tick-by-tick for those 6 h (player shadowing him) ends at `(747.5, −1383.1)` — **0.7 m apart** (`ARRIVE_EPS` 1.5). Travellers are re-entry-checked on their analytic position (`npc.ts:171-181`) — good.
- Freeze: at Altdorf noon 28 NPC meshes, 385 far NPCs all `frozen`, none unfrozen; walking 5 km away leaves `dynamic root children=0, MeshRef components=0`; `clear()` → 0 Npc entities, 0 children. No mesh leak across freeze/clear. `maxVisibleCrowd = 60` cap (`npc.ts:44`).
- Water: Jost walking Flüelen→Altdorf across a fake water band stops at `z=1699.7` (band starts at 1700) — never wades.
- `populate(chapter)`: prologue spawns the three Rütli men, Attinghausen, Jost; not Tell/Gessler/Landenberg/Leopold/Hünenberg/Winterthur/Ludwig/Abt Johannes. ch1→ch2 despawns Gessler/Tell, spawns Leopold; entity count 418→418 (no accumulation). `spawnNamed` goes through `party.createCharacter` (real Character/Skills/Equipment entities).
- Interaction `interact.ts:15-16, 30-48`: 2.5 m, 60° cone via `wrapAngle(bearing − yaw)`, nearest wins; `E` in `explore` state (`index.ts:271`); `talk` → `quest.runDialogue(dialogueId, entity)`; `loot` → `ui.openMenu('container')`; containers: one per castle (`bolzen/torch/rope/bread` + 20 Pf), monastery (`herbs/psalter/bandage`), inn village (`bread/alpkaese/wine/dried-meat`).
- HUD feed `hud.ts`: hp/morale/fatigue/time/season/region/compass (discovered markers + one `?` for nearest undiscovered) + prompt — visible in both screenshots (bars bottom-left, compass top, date/region top-right).
- Settlements `settlements.ts`: one `generateLayout` per settlement-kind POI, meshes baked per **material instance** and merged once globally → `altdorf-square-noon` 638 calls, `schwyz-village-dusk` 470 calls (budget 2 000). Gallows pole kept unmerged, hat `Group` hidden unless `lastChapter === 'ch1-1307'` (`settlements.ts:110-121`); pole spot `(586, 2045)` is `settlement` surface, slope 0°.
- Habsburg patrols: 10 footmen on three road chains, lead-only trigger, gated on `quest.isHostile('habsburg')` (`hostileBelow` rep or `hostile:habsburg` flag). Probe: not hostile → no call; hostile → `startEncounter('enc.altdorf-square')` fires at Arth `(−1471, −2448)`. Combat never reads `EncounterDef.location`, so the fight happens where the player is — but it is always "The Square at Altdorf" with 4 bailiff guards and two Uri militia allies, even on the Küssnacht road.
- `'loaded'` / `→loading`: teardown disposes the player mesh and calls `npcSystem.clear()` (destroys every Npc entity **and** clears `patrols`/`patrolLead`); `loaded` only re-ensures the player mesh. Because `setDiscovered()` is applied by the save module and Poi entities come back from the ECS snapshot, discovery survives a load — but a chapter change calls `spawnPoiEntities()` (`poi.ts:43-52`) which recreates every Poi with `discovered:false` and nobody re-applies the set → **all map discoveries are wiped at each time-skip** (probe: `poi.ruetli still discovered after a repeat populate(): false`).

Screenshots:
- `altdorf-square-noon.png`: reads as a village square — stone well with shingle roof, plaster church wall with windows, log houses with shingle roofs, fences, trees on the slope, HUD, `[E] Talk to Bailiff's Guard`. ~17 figures clustered within ~15 m of the well, all facing the same way, all identical capsule-with-sphere bodies; a `bailiff-guard` in white surcoat + red legs (combat's renderer) and two green militia with kettle hats are the only distinguishable ones. The player is the brown-hooded capsule at bottom centre. No pole/hat visible in frame (prologue: hat correctly hidden; pole is 12 m east, out of shot). No trace of a lime tree or Landsgemeinde ground.
- `schwyz-village-dusk.png` (19:05, clear): correctly dim warm-grey dusk, long shadows gone, church wall and log houses read, crowd of ~13 around the well, `[E] Talk to Village Elder`, region "Schwyz basin". The Mythen are not visible behind the church (camera too low); the scene is legible but generic — nothing says "Schwyz" rather than "Altdorf".
- `ruetli-dawn.png`: three figures on a dark brown slope above the lake, one with a kettle hat (Melchtal's `militia-halberd` model) — the three men are there, but the meadow is bare `road`-surface dirt at h 50.7 m (gazetteer h 22), no trees, no boat, no cross, and the far shore is a black wall; not a place a player would recognise as the Rütli.
- Placeholder geometry: none per se (no `placeholder` model instances), but every human is the same three-primitive capsule; NPCs do not animate (only the player's limbs swing).

## Historical compliance (content modules)
- `poi.ruetli` (meadow, L oath) → LORE §3/§4 — place is H, file says `'legend'` (note explains) — minor field mismatch
- `poi.altdorf`, `poi.buerglen`, `poi.steinen`, `poi.gesslerburg`, `poi.rotzberg`, `poi.landenberg` → LORE §3 lists the *places* as H (L only for the pole/house/storming/bailiff); files say `'legend'` — 7 defs where the H/L line is blurred the conservative way; notes are correct. Fix: `historical: true` + keep the note, or add a LORE §10 line stating the convention "a POI is `legend` when its game role is L".
- `poi.tellsplatte`, `poi.hohle-gasse`, `poi.zwing-uri`, `poi.melchtal` → LORE §3 L ✓
- `poi.aegerisee-shore` → LORE §4 mandated; sited on the **west** shore `(−122, −3746)` in region `zug` while LORE §3 puts "Ägerisee south shore" under `schwyz-arth-morgarten` (south end is z ≈ −3460 by Morgarten) — misplaced
- 10 invented POIs → LORE §10 row ✓ (ids match exactly)
- `poi.pilatus` / `poi.rigi` as `alp` at the summit coordinates (`pilatus` h 565, `rigi` = Rigi Kulm) — LORE §4 says "Pilatus alp", "Rigi alp"; the gazetteer's `fraekmuentegg` is the actual Pilatus alp. Acceptable, but an alp hut on the Pilatus summit is odd siting.
- `npc.werner-stauffacher` (legend), `walter-fuerst` (legend), `arnold-von-melchtal` (legend), `wilhelm-tell` (legend, ch1), `hermann-gessler` (legend, ch1), `beringer-von-landenberg` (legend, ch1), `werner-von-attinghausen` (H), `leopold-i` (H, ch2), `heinrich-von-hunenberg` (legend, ch2), `johannes-von-winterthur` (H, ch2) → LORE §5 ✓
- `npc.abt-johannes` (H) → chapters `['ch1-1307','ch2-1314']`; the brief says ch2 only, but LORE §2 makes him abbot 1298–1327, so ch1 presence is historically right — accepted, note it.
- `npc.konrad-ab-yberg` → LORE §5 "H (family; individual I)" but file `historical: 'legend'` — **should be `'invented'`** (the note itself says "I")
- `npc.ritter-eberhard-von-mulinen` → LORE §5/§10 id is `npc.ritter-eberhard-von-mülinen`; file, dialogue (`named-cast.ts:345`) and quest content all use the ASCII form. ARCHITECTURE §1 wants kebab-case slugs, so the ASCII id is the sane one — append a LORE §10 line recording the id spelling rather than leaving the register pointing at a non-existent id.
- `npc.jost-imhof`, `mechthild-schorno`, `heini-odermatt`, `bruder-anselm`, `ueli-zgraggen`, `vogt-schreiber-ludwig` → LORE §5 / §10 ✓ (Anselm's faction `unterwalden` and Ueli's `none` are noted in their `note`s)
- `npc.heinrich-von-melchtal` (legend) → LORE §1/§5 ✓, registered in §10
- 79 minor NPCs → LORE §10 row ✓; `npc.bruder-toni`, `npc.bruder-sepp` are `abbey-man-at-arms` yet named "Bruder" (LORE §8: address for monks) — rename (e.g. "Toni Kälin", "Sepp Schuler") or make them monks
- Banned anachronisms (§7) player-facing: none found in any description/name/equipment.

## Ranked issues (most damaging first)

1. **Three invented POIs are on the lake bed and get no geometry** — evidence: `terrain-siting` on `buildHeightGrid(1291)`: `poi.wegkreuz-axenweg surface=water h=−20.2`, `poi.klausnerzelle surface=water h=−4.1`, `poi.fischerhuetten-gersau surface=water h=−17.7`; `settlements with NO built layout: 4 → poi.treib, poi.wegkreuz-axenweg, poi.klausnerzelle, poi.fischerhuetten-gersau`. The player swims out to "discover" a hermit's cell 4 m underwater. Cause: offsets in `src/content/pois.ts:337` (`sisikon −120,−260` → mid-Urnersee, the Axen shore is *east* of Sisikon), `:359` (`bauen +380,+320`), `:365` (`gersau −420,+260` → 135 m out in the basin). Fix: move them onto land — `wegkreuz-axenweg` at `sisikon +40,−150` (on the Axen path), `klausnerzelle` at `bauen −60,+140` (cliff above Bauen), `fischerhuetten-gersau` at `gersau +120,−40` (shore east of the village) — and add a test in `content.test.ts` that samples `buildHeightGrid` (3 s, as `wave1-world.md` recommends) asserting `!isWater` for every POI whose kind is not `port`/`landmark`. Treib (`port`, real gazetteer point on the water) should also nudge its quay hut inland: `layoutPort` places `house.blockbau` at `(−12, −6)` which is still water there.
2. **Zug and Küssnacht are heaps of interpenetrating buildings** — evidence: `poi.zug 70 buildings; 53 have a neighbour < 3 m away; footprint radius 57 m; water at 60 m in 7/8 directions`, `poi.kuessnacht 13 buildings; 5 stacked; water at 60 m in 8/8 directions`, `overlapping building pairs across the map: 861` (Zug alone 771, Luzern 47, Zwing Uri 3, Rotzberg 2). Cause: `findDrySpot` (`layout.ts:31-41`) pulls every wet/steep candidate toward the centre by ×0.7 up to six times and never checks the spot against already-placed buildings, so a town on a peninsula folds onto itself. Fix: (a) in `Builder.add`, reject a spot closer than `RADIUS[modelId] + RADIUS[other]` to any placed model (the collider table in `colliders.ts` already has the radii) and try the *tangential* direction (rotate the offset ±30°, ±60° around the centre) before pulling inward; (b) for `town`, compute the wall half-size from the dry area (`half = min(55, dryRadius − 8)`) instead of a fixed 55 m; (c) add `layout.test.ts` cases with a half-water probe asserting zero overlapping colliders. The world builder could also widen the Zug/Küssnacht land pads (`geodata` settlement flattening), but the layout must not depend on that.
3. **`populate()` destroys recruited companions at every chapter time-skip** — evidence: `npc-system` probe `companion entity 6 survives chapter change: false`; `NpcSystem.clear()` (`npc.ts:62-72`) destroys every entity with `Npc`, and `quest/index.ts:225-227` recruits companions by giving the *spawned NPC entity* a `PartyMember` (`addCompanion: 'npc.jost-imhof'`, `mechthild-schorno`, `heini-odermatt`, `bruder-anselm`, `ueli-zgraggen` in `dialogues/named-cast.ts`); `quest.setChapter()` calls `exploration.populate(chapter)` (`quest/index.ts:386`) *and* emits `chapter-changed`, which `index.ts:71` turns into a second `populate`. `party.getParty()` is a `PartyMember` query, so the companion silently vanishes from the party at 1291→1307 and 1307→1314. Fix: in `clear()` skip entities that have `PartyMember` (`if (this.world.has(id, PartyMember)) continue;`) and in `populate()` skip a named def whose entity already exists in the party (`defIdOf` lookup) so it is neither duplicated nor re-homed; keep `chapters` respected for non-party NPCs; add a probe-style test. Also drop one of the two populate calls (the `chapter-changed` listener in `index.ts:71` is redundant with the direct call — `newGame` currently populates three times: `main.ts:115-117` + `setChapter` + event).
4. **98 crowd entities have a talk prompt that opens nothing** — evidence: probe `NPC entities whose fallback dialogue id does NOT exist: dlg.generic.woman-peasant×27, dlg.generic.fisher×27, dlg.generic.habsburg-footman×22, dlg.generic.militia-spear×6, dlg.generic.habsburg-sergeant×6, dlg.generic.bailiff-guard×4, dlg.generic.militia-halberd×3, dlg.generic.abbey-man-at-arms×2, dlg.generic.habsburg-crossbowman×1` (98 of 418 entities in ch1); `generic.ts` defines only `peasant, innkeeper, priest, boatman, saeumer, monk, habsburg-guard, toll-collector, herder, child, merchant, elder`. `runDialogue` on an unknown id logs `[dialogue] unknown dialogue` and returns — the very prompt in `altdorf-square-noon.png` (`Talk to Bailiff's Guard`) is one of them. Fix in `npc.ts:98,128`: map archetype → generic id (`woman-peasant→peasant`, `fisher→boatman`, `militia-*→peasant`, `habsburg-*|bailiff-guard|abbey-man-at-arms→habsburg-guard`) and fall back to `dlg.generic.peasant` when `content.dialogues.has()` is false; `interact.ts` should also `ui.toast` instead of silently returning when the id is missing.
5. **No boat `travel` interactables** — §5.2 "Boats at Flüelen/Brunnen/Gersau/Luzern as `travel` interactables" and the rubric's "boats/fast travel": `layoutPort` places `boat` models only; no `Interactable{kind:'travel'}` exists anywhere in `src/exploration` (grep). Fix: in `spawnContainers`-style pass, for each `port` POI add an entity at the first boat's position with `Interactable{kind:'travel', prompt:'Take the boat', data:{destinations:[…ports across the lake]}}`; `interactWith` → `ui.openMenu('travel')` or a simple choice → `fastTravel(dest)` (which already advances the clock).
6. **`fastTravel()` is ungated in the service** — evidence: source probe (`index.ts:181-194`). Any caller (harness, quest effect, a future UI) can jump to an undiscovered or `fastTravel:false` POI. Fix: `if (!this.isDiscovered(poiId) || !this.poiDef(poiId)?.fastTravel) { ui?.toast('You have not found that place yet.'); return; }` plus a unit test.
7. **NPC "life" is static** — evidence: `NPCs whose schedule visits >1 POI: 8/98`; all 314 crowd members and 71 minor NPCs have every entry `poi:'home'` with the same spawn offset, so they never take a step; `daySchedule` `sleep` at 22:00 keeps them standing in the square (the 23:00 Sarnen capture will show this). The rubric's 8-bar asks for "NPCs with schedules"; these are schedules in name only. Fix (cheap, big payoff): give `daySchedule()` distinct offsets per activity (work: near a random house from the layout, market: the well ±8 m, tavern: the inn house, sleep: *inside* a house — `Renderable.visible=false` or move to the house pad so they disappear indoors); let `stepSchedule` set `Interactable.enabled=false` while `activity==='sleep'`. Add a walk-cycle to `humanoid.ts` figures (the player already has one in `playerModel.ts`; the merged crowd model has no limb pivots — keep the merged body but swap to the pivoted model for the ≤ 28 near NPCs).
8. **Map discoveries are wiped at each chapter change** — evidence: probe `poi.ruetli still discovered after a repeat populate(): false`; `spawnPoiEntities()` (`poi.ts:43-52`) destroys and recreates every `Poi` with `discovered:false` and `populate()` (`index.ts:76-81`) never re-applies `discoveredIds()`. Fix: `const keep = this.discoveredIds();` before the loop and `this.setDiscovered(keep)` after, or don't destroy Poi entities at all on a repeat populate (only create the missing ones).
9. **Patrol / settlement state is not rebuilt after a load** — `teardownTransientMeshes()` calls `npcSystem.clear()`, which also empties `patrols`/`patrolLead` (`npc.ts:69-70`); the 10 patrol entities come back from the ECS snapshot with `activity:'patrol'` but no `PatrolState`, so after a load they stand still and the hostility trigger can never fire; `settlementsGroup` is never rebuilt on `loaded` either (it survives only because `resetWorld()` clears the ECS, not the scene). Fix: on `'loaded'`, rebuild `patrols` from entities whose `defId` starts with `patrol.` (store road id + index in `Npc.defId`), and call `rebuildSettlements()` if `settlementsGroup.children.length === 0`. Also `checkPatrolTrigger` uses `performance.now()` with `lastTrigger = 0`, so no patrol can trigger in the first 15 s after page load (probe: `hostile, adjacent to lead patrol at t=5 s: startEncounter calls=0`) — initialise `lastTrigger = −Infinity`.
10. **Building pads accept slopes up to 49°** — `MAX_SLOPE_DY = 3.5` over a 3 m step (`layout.ts:27`) = 49.4°, while the player is blocked at 40°; probe: `houses placed on a 45° slope: 9`; on the real grid `7/562` pads exceed 40°. Fix: `MAX_SLOPE_DY = 1.6` (≈ 28°) and test it.
11. **Patrol encounters always run "The Square at Altdorf"** — `npc.ts:300-301` reuses `enc.altdorf-square` (4 bailiff guards + 2 Uri militia allies + a herder) for a road patrol at Arth or Göschenen. Fix: ask combat/content for a `enc.habsburg-patrol` (3 footmen, no allies, grid at the player) — this is a cross-module request (`requests/exploration-1.md`), not something to hack here.
12. Content nits (LORE list above): `konrad-ab-yberg` → `'invented'`; `mülinen` id spelling vs LORE §10; "Bruder" on two men-at-arms; `poi.aegerisee-shore` to the south end (e.g. `morgarten −120,−140`, region `schwyz-arth-morgarten`); seven real places marked `'legend'`; discover-disc overlaps (Rütli/Treib, Gesslerburg/Küssnacht, Sarnen/Landenberg, Amsteg/Zwing Uri) — shrink the secondary radius so arriving at one does not toast both.
13. Cosmetic: `disposeObject3D` in `settlements.ts`/`npc.ts` calls `material.dispose()` on the shared cached materials from `world/models.ts`/`humanoid.ts` (still referenced by every other mesh) — wasted GPU re-init on every freeze; only dispose geometry. `spawnNamed` jitter can put a named NPC inside a house collider (no check). `layoutVillage` has 1 overlapping pair in every village (the church at `(0,−16)` vs the ring house nearest to it — push the ring to `radius ≥ 24` or skip the angle behind the church).

## Explicitly out of reach for a browser engine (not counted against the score)
- Skyrim-grade hand-dressed settlements, unique building kits per canton, interiors, and animated crowd rigs (the three-primitive humanoid and merged static crowd are the right budget call for SwiftShader/WebGL).
- Navmesh-based NPC pathing along roads and around buildings; straight-line terrain-following walks are acceptable at this stage.
- Ambient herds / boats crossing the lake / NPC chatter (rubric 10/10 items).
- The "Rütli that looks like the Rütli" depends on the world module's shore fix (`wave1-world.md` issue 1) — the bare 12° `road` slab at h 50 m is not exploration's doing.

## What would move this to 8 (round 2 checklist)
Fix 1–6 (siting of the three wet POIs, Zug/Küssnacht pile-up, companion wipe, generic-dialogue fallback, boat travel interactables, service-side fast-travel gate) and show them on evidence: a harness scenario at Zug (`free`-camera 80 m above `poi.zug`) with `overlaps 0` from `layout.test.ts`, a probe-grade test for the companion survival, and `menu-map` / a boat prompt in a screenshot. Issue 7 is what separates 8 from 9.
