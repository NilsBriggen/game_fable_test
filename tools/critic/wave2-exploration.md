# Wave 2 — exploration — critic score (round 2/3)

Reference: Skyrim (discovery loop)
Harness run: `altdorf-square-noon` 2026-09-02T21:38:11Z (`PASS calls=629 tris=1656818 heap=156MB err=0 warn=0`, entities 523); `sarnen-night-rain` 2026-09-02T21:48:31Z (`calls=158 tris=5433205 heap=321MB err=0 warn=0`, entities 534 — harness FAIL on the *triangle* budget only, see below); `morgarten-winter` **not captured** (coordinator called time; judged from code only) — each alone on the harness lock, `--out tools/harness/out/critic-exploration/<id>`, renderer `Google Inc. (Google) / ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)` (SOFTWARE — frame times are an upper bound). Round-1 captures (`schwyz-village-dusk` 20:40Z, integrator `menu-map` 20:58Z / `dialogue-gessler-hat` 21:06Z) still stand where nothing changed. Code reviewed at HEAD `108d0d1` (fix commit `9e5688f` = all 14 round-1 items; `77e2e7b` = companions follow, named-NPC daily offsets, trade/rest interactables), full diff `81b6069..HEAD` for `src/exploration/**`, `src/content/{pois,npcs,encounters}.ts`, `src/core/services.ts`, `LORE.md` read line by line. Two art builders are concurrently replacing the world look and the character models (`src/world/{assets,sky,terrainMaterial,water}.ts` dirty in the working tree); this sheet judges exploration on placement, life, discovery and interaction and says explicitly what is world/art.

Evidence read:
- `npx vitest run src/exploration src/world/poi-siting.test.ts 2>&1 | tail -7` → ` ✓ src/world/poi-siting.test.ts (2 tests)` / ` Test Files  7 passed (7)` / `      Tests  62 passed (62)` (52 → 62: new `fixes.test.ts` ×8 + `poi-siting.test.ts` ×2).
- `npx tsc --noEmit 2>&1 | grep -E 'src/(exploration|content/(pois|npcs))'` → no output. The only tsc error in the tree is `src/world/sky.ts(71,30): error TS1005` — the sky art builder's uncommitted WIP, not this module. `node tools/check-imports.mjs` → `imports ok`.
- My probes, re-run against HEAD (`npx vitest run --config tools/critic/probes/exploration/vitest.config.ts` → `Tests 41 passed (41)`): the 32 round-1 probes with the five bug-documenting assertions inverted (each now asserts the fixed behaviour) plus 9 new round-2 probes (sleep hidden/not-interactable at Sarnen 23:00, crowd market offsets, companion not duplicated, companion following a moving player, named-NPC offsets, `rebindPatrols`, boat travel entities, Zug/Küssnacht composition on the real grid). `npx vitest run src/exploration src/world/poi-siting.test.ts` re-run at `108d0d1` → `Tests 62 passed (62)`; tsc grep on the module clean; `imports ok`. Every number below is from these runs, not from the fix-round summary.
- PNGs read: `tools/harness/out/critic-exploration/altdorf-square-noon/altdorf-square-noon.png` (21:38), `tools/harness/out/critic-exploration/sarnen-night-rain/sarnen-night-rain.png` (21:48), plus the round-1 set (`schwyz-village-dusk`, `ruetli-dawn`, `dialogue-gessler-hat`, `menu-map`).

## Score: 8/10   (pass bar 8)  → PASS (round 2/3)

Every clause of the rubric's 8-bar now holds on captured or probed evidence: 94 POIs on real geography (coordinates 0 m off the gazetteer, every non-port/landmark centre on land on the real height grid), discovery that fires once with toast + compass + map marker and survives chapter changes, NPCs with real schedules in 73 settlements — the crowd moves between work/market/tavern spots and goes indoors at night (the Sarnen capture at 23:05 shows an empty rain-swept square where round 1 would have shown 17 figures standing in the rain), interaction prompts for talk/loot/travel/trade/rest with every dialogue id resolving, boats at all five ports plus a gated fast travel, 40° terrain blocking in the controller, and zero console/page errors in all three of my captures. All 14 round-1 issues are fixed in code and re-proven by probe; the two consequences I found during verification (companions did not follow; named minors stood still) were fixed by the integrator in `77e2e7b` before this sheet closed. What keeps it at 8 rather than 9: Zug — a mandated Habsburg town — is a five-house walled hamlet because the world's Zugersee swallows its pad; two relocated invented POIs have centres on 44°/51° slopes; the 71 minor named NPCs shuffle only a few metres through their day; and the Sarnen capture blows the 3 M triangle soft budget (5.43 M at 158 draw calls) — that last one is the art builders' new vegetation/rain, not settlement geometry (Altdorf with the same settlement pass is 1.66 M), and is called out as world/art below.

## Round-1 issues → status (all verified on code + probe, not on the summary)

| # | Round-1 issue | Status | Verified at |
|---|---|---|---|
| 1 | Three invented POIs on the lake bed; Treib empty | **Fixed** | `pois.ts` offsets `sisikon −20,−140`, `bauen −60,+140`, `gersau +100,+10`; `src/world/poi-siting.test.ts` (real grid) ✓; my `terrain-siting`: `POI centres in water (1): poi.treib(port)` only; `settlements with NO built layout: 2 → poi.treib(port), poi.steinbruch-axen(camp)` (was 4). Treib's quay hut is still skipped (gazetteer point is water; port kind is exempt) — see new issue 3. |
| 2 | Zug/Küssnacht heaps (861 overlapping pairs) | **Fixed** | `layout.ts` `Builder.add` rejects water/steep/overlap with ±30/60/90° rotation and 0.55–1.0 shrink; `dryRadius(60)` sizes the town wall; village ring 24 m with the church sector open. Probe on the real grid: `overlapping building pairs across the map: 0`; `poi.zug 15 buildings; 0 stacked; footprint radius 40 m`; `poi.kuessnacht 11 buildings; 0 stacked`; `poi.luzern 71 buildings; 0 stacked`. Zug on flat land would be `house.stone×19, castle.wall×50`; on its real peninsula it is `chapel×1, well×1, house.stone×5, castle.wall×9` (16 of 71 requested) — the heap is gone, but the mandated Habsburg town is now a hamlet (new issue N3). 944/1014 requested models placed map-wide (dropped rather than piled). |
| 3 | `populate()` destroyed recruited companions | **Fixed** | `npc.ts:73` skips `PartyMember` in `clear()`, `populate()` skips defs already in the party via `defIdOf`. Probe: `companion entity 6 survives chapter change: true`, `Jost entities after the time-skip: 1` (no duplicate). The consequence I found (companions were then simulated by their own schedule and never followed) is fixed in `77e2e7b`: `stepFollow()` trails the player at 2–3 m in three slots, snaps if > 60 m behind, never freezes. Probe: `companion follow: after a 400 m walk distance to player 2.8 m (worst 2.8 m), frozen=false, mesh=true`. |
| 4 | 98 crowd entities with a dangling `dlg.generic.*` | **Fixed** | `npc.ts` `GENERIC_DIALOGUE` map + `genericDialogue()` checks `content.dialogues.has()`, falls back to `dlg.generic.peasant`; `interact.ts` honours `quest.dialogueExists?.()` (not implemented on the quest side, so the check is a no-op — harmless because the mapping already guarantees existence). Probe: `NPC entities whose fallback dialogue id does NOT exist: none`; all 25 bespoke `dialogueRoot`s exist among the 54 defined dialogues. |
| 5 | No boat `travel` interactables | **Fixed** | `interact.ts` `spawnBoatTravel()` — one `Interactable{kind:'travel', prompt:'Take the boat'}` per port (5), destinations = other ports nearest-first, `travel()` offers the discovered ones via `ui.confirm` then `fastTravel()`. Probe: `ports 5, travel entities 5; Flüelen boat → poi.treib, poi.brunnen, poi.stansstad, poi.alpnachstad`; idempotent on repeat populate. Not seen on a capture (no scenario spawns at a port facing the boat). |
| 6 | `fastTravel()` ungated in the service | **Fixed** | `index.ts:194-198` refuses unless `isDiscovered && def.fastTravel`, toasts `You have not found that place yet.`; source probe `gated: true`. |
| 7 | NPC life static (everyone stood on spawn jitter 24 h) | **Fixed (crowd + indoors at night), partial (named minors)** | Generic crowd now has work / market (11–13 h, well ±4–10 m) / tavern (17–18 h) / sleep (21–22 h, out toward the houses) entries with distinct offsets: probe `generic NPCs with a market spot > 3 m from their work spot: 296/314`; `stepSchedule` hides the mesh and disables the `Interactable` once asleep: `Sarnen 23:00 — near NPCs 21, hidden 20, not interactable 20`; `Sarnen 12:00 — visible 21, interactable 21`. Named NPCs: `77e2e7b` made `withDefaultOffset` activity-specific (market 0.35×, tavern 0.6×+3 m, church −8 m, sleep 1.6× of the spawn jitter), so they now step a few metres between activities and vanish indoors at night — but because the jitter radius is ≤ 6 m the moves are small: probe `named NPCs with a work entry: 73; market spot > 3 m from work: 18; sleep spot > 3 m from work: 20`. Named traveller count unchanged at `8/98`. Capture: `sarnen-night-rain.png` at 23:05 shows the square empty except the player — the crowd is indoors. Crowd animation is the character-art builder's (the Altdorf capture already shows their posed rigs). |
| 8 | Discoveries wiped on chapter change | **Fixed** | `poi.ts:44` keeps `discoveredIds()` across `spawnPoiEntities()`; probe `poi.ruetli still discovered after a repeat populate(): true`. `populate()` is now idempotent per chapter (`index.ts:80`), so the triple populate on `newGame` is gone. |
| 9 | Patrol/settlement state lost after load; 15 s dead zone | **Fixed** | `npc.ts` `rebindPatrols()` from `Npc.defId = patrol.<road>.<i>` (probe: `after rebind: patrols=10 leads=3`), called from `rebuildTransientMeshes()`, settlements rebuilt if the group is empty; `lastTrigger = -Infinity` (probe: `hostile, adjacent to lead patrol at t=5 s: startEncounter calls=1`). |
| 10 | 49° building pads | **Fixed** | `MAX_SLOPE_DY = 1.6` (≈ 28°); probe `houses placed on a 45° slope: 0`; real grid `pads on > 40° ground: 2/499` (was 7/562; the two are `isGentle`'s 3 m probe missing a ridge — negligible). |
| 11 | Patrol fights ran "The Square at Altdorf" | **Fixed** | `content/encounters.ts` `enc.habsburg-patrol` (sergeant + 2 footmen, `heightOverride: 'flat'`, `rout`), `index.ts:58-64` passes `encounterOverride` with `location` = player position (`CombatService.start` already accepts it). Probe: `startEncounter calls: [["enc.habsburg-patrol",{x:-1469.2,z:-2446.6}]]` at the Arth road, position == player. |
| 12 | Content nits | **Fixed** | `konrad-ab-yberg` → `'invented'`; `bruder-toni/sepp` → `npc.toni-kaelin` "Toni Kälin", `npc.sepp-schuler` "Sepp Schuler"; Rütli/Altdorf/Bürglen/Steinen/Gesslerburg/Rotzberg/Landenberg → `historical: true` with a LORE §10 convention row; LORE §10 row for the `mulinen` id; `poi.aegerisee-shore` → `morgarten −120,−140` = `(218, −3476)`, region `schwyz-arth-morgarten` (lake's south vertex z = −3460 ✓). Discover-disc overlaps not addressed (now 7 pairs: `aegerisee-shore~morgarten` and `fischerhuetten-gersau~gersau` added) — cosmetic, carried as N4. |
| 13 | Gessler pole out of frame | **Fixed in code** | `settlements.ts:119` pole at `(+3, −8)` between well and church, in the default north-facing view. Capture `altdorf-square-noon.png` (21:38): a thin dark pole stands right of the well against the church wall, in frame from the default spawn; no hat — correct for the prologue (hat only in `ch1-1307`). Still easy to miss at 6 m: the pole is 0.12–0.16 m thick and unlit; a wider base/plinth or the lime tree beside it would anchor it. |
| 14 | `material.dispose()` on shared materials; village church overlap | **Fixed** | both `disposeObject3D`s are geometry-only; village ring at 24 m with the sector behind the church (`0,−16`) left open — probe: `Altdorf 15 buildings, 0 stacked`, 0 overlapping colliders anywhere. |

## What the evidence shows (bullets, cite screenshot or number)

Content (unchanged where not listed; probe `content-audit.test.ts` 11/11):
- 94 POIs, 30 mandated present with kind/coordinates (`max coordinate deviation 0 m`) and region; `historical` now matches LORE §3 for every mandated place under the new §10 convention (`historical-vs-LORE contradictions` for the *place* status: only the four sole-legend places remain `legend`, as intended). 98 NPCs, 12 H/L + 7 invented core + 79 minors; `Schiller-only names: none`; `"Bruder" used for non-monk archetypes: none`; banned-word grep on NPC descriptions: none (POI descriptions still carry "canton" in two UI strings — permitted by §7).
- Invented: 10 POIs + 79 minors + the two new LORE §10 rows (id spelling, `historical` convention) ✓.

Code:
- Everything in the round-1 "code" bullets still holds (speeds/slope/jump/swim/fatigue, 6 m collision-adjusted camera, discovery once + toast + compass + region toast, analytic re-entry within 0.7 m of stepping, no wading, 300 m freeze with zero mesh leak across freeze/clear, 2.5 m / 60° interaction, containers, HUD feed, hat by chapter, draw calls merged per material).
- New: boat travel entities at 5 ports; `fastTravel` gate; sleep → hidden + not interactable; `rebindPatrols`; `enc.habsburg-patrol` at the player; idempotent `populate`; `spawnTradeAndRest()` — `Rest at the inn` at every inn village, `Trade at the market` at every merchant/town POI, wired to `ui.openMenu('trade'|'rest')` which `ui/menus.ts:692-693` implements.
- Companions follow (`npc.ts` `stepFollow`, `77e2e7b`): a `PartyMember` NPC is never frozen, is unfrozen next to the player on first tick, walks at `min(6.5, 2 + d)` m/s to a slot 2.2 m behind the player, and is snapped if > 60 m away (fast travel). Probe above. Not yet seen on a capture (no scenario recruits a companion).

Screenshots:
- `altdorf-square-noon.png` (mine, 21:38, 1 Aug 1291 12:05): the square reads better than in round 1 — well, church wall, three log houses with plaster gables, a hayrack, the gallows pole right of the well, compass with the Flüelen anchor, HUD, date/region. ~20 figures, now the character-art builder's rigged models: kettle-hatted militia with spears, a bailiff's guard with sword and shield, a black-clad figure with a staff, purple/green/tan cloth — NPCs are finally distinguishable at a glance, and several are caught mid-stride (the crowd's new market/tavern offsets give them somewhere to walk). **World/art, not exploration**: the ground is uniform white at noon in August (terrain-material/snow WIP — `src/world/terrainMaterial.ts` is dirty in the tree), the sky is blank white, and the player at bottom-left is a new rigged model. No interaction prompt in frame (no NPC within 2.5 m/60° of the spawn — fine).
- `sarnen-night-rain.png` (mine, 21:48, 1 Aug 1291 23:05, rain): a genuinely night scene — near-black village, rain streaks, house silhouettes and the well roof, dark peaks and a blue-black sky behind, region "Obwalden", compass with one discovered marker. **The square is empty** except the player at the well: every Sarnen NPC is asleep and hidden (probe: 20 of 21 hidden) — exactly the round-1 issue-7 fix, now visible. Harness marks the scenario FAIL for `triangles 5433205 > 3000000` with only 158 draw calls / 110 geometries / 5242 instances: **world/art** — the same settlement pass costs 1.66 M at Altdorf, and `src/world/vegetation.ts`/models changed in `108d0d1`; the world/art critic should take that number. Zero console/page errors.
- `morgarten-winter`, `gotthard-schoellenen`: not captured this round (coordinator's time limit). From code: Morgarten is a `battlefield` (no built layout), `sattel-letzi` places three `letzi.wall` segments across the road yaw; the Teufelsbrücke `bridge.stone` is placed at the road yaw + 90° — the gorge itself is the world builder's.

## Historical compliance (content modules)
- Mandated POIs → LORE §3/§4 ✓ (`historical` per the new §10 convention: attested places `true`, sole-legend places `legend`)
- `poi.aegerisee-shore` → LORE §4, now at the south end in `schwyz-arth-morgarten` ✓
- 10 invented POIs → §10 ✓ (relocated ones on land per `poi-siting.test.ts`)
- LORE §5 cast → ✓ incl. `konrad-ab-yberg` `'invented'`, `abt-johannes` ch1+ch2 (historically right), `ritter-eberhard-von-mulinen` id now registered in §10
- Companions and lieutenants → §5/§10 ✓; 79 minors → §10 ✓ (`toni-kaelin`, `sepp-schuler` renamed)
- Banned anachronisms (§7) player-facing: none.

## Ranked issues (open)
N1. ~~Recruited companions are scheduled NPCs, not followers~~ — **fixed in `77e2e7b`** (`stepFollow`), probe-verified (2.8 m behind a moving player after 400 m). Remaining nit: the follow slot is `(id % 3) − 1`, so two companions can share a slot; use the party slot index instead.
N2. **Minor named NPCs move only a few metres a day** — `withDefaultOffset` scales the ≤ 6 m spawn jitter, so 55 of 73 named NPCs' market spots are within 3 m of their work spot. Fix: anchor activities to *layout* features instead of the jitter — innkeeper/tavern at the `inn` house pad, priest at the church, market at the well ± 8 m, sleep at the nearest `house.blockbau` pad (the layout is available in `settlements.ts` at populate time; pass a per-POI `{well, church, inn, houses[]}` map into `NpcSystem.populate`).
N3. **Zug is a five-house hamlet** — evidence: real-grid probe `poi.zug: {chapel:1, well:1, house.stone:5, castle.wall:9}`, `water at 60 m in 7/8 directions`. The world's Zugersee swallows the gazetteer point (`zug` h −5, inside the lake polygon), so `dryRadius(60)` finds ~40 m of land and the town shrinks to fit. Two-sided fix: exploration should search for the driest centre within 150 m (max of `dryRadius` over a 5×5 grid of candidate centres) before laying out a `town`, and the world builder should give Zug/Küssnacht a flattened dry pad ≥ 120 m (as it does for Altdorf/Schwyz/Sarnen, all `surface=settlement`, slope 0°). Same for Küssnacht (`water in 8/8 directions`, 10 houses on 44 m). Cross-module: file `requests/world-<n>.md`.
N3b. **Treib and Steinbruch am Axen have no geometry; two relocated POIs sit on cliffs** — `poi.treib(port)` centre is water on the real grid (gazetteer h 0) so `layoutPort`'s hut/cross are skipped (only 3 boats); `poi.steinbruch-axen(camp)` sits on 30° scree so campfire+tent fail the 28° gate. Fix: `layoutPort` should place the hut with `dryRadius`-style search up to 40 m inland; give `camp` a 35° allowance or move the quarry 60 m downslope. The relocated `poi.wegkreuz-axenweg` centre is on a 44° slope and `poi.fischerhuetten-gersau` on 51° (`surface=road`, h 2.5) — the models were rotated onto gentler ground by `Builder.add`, but the POI centre itself (where `nearestPoi`/markers point and where fast travel could drop a player if it were enabled) is unwalkable; nudge each 20–30 m toward the shore path and add a slope assertion to `poi-siting.test.ts` (`slopeDeg(centre) ≤ 30` for non-landmark kinds).
N4. Cosmetic: 7 overlapping discover discs (`ruetli~treib`, `gesslerburg~kuessnacht`, `morgarten~sattel-letzi`, `landenberg~sarnen`, `amsteg~zwing-uri`, `aegerisee-shore~morgarten`, `fischerhuetten-gersau~gersau`) — arriving at one toasts both; shrink the secondary radius (`cross`/`hut`/`wall`/`landmark` 20–40 m). `quest.dialogueExists` is declared optional and unimplemented — implement it (`this.ctx.content.dialogues.has(id)`) so `interact.ts`'s guard actually guards bespoke `dialogueRoot`s. `spawnNamed` jitter still ignores house colliders.
N5. World/art, recorded for the other critic: `sarnen-night-rain` triangles 5.43 M (budget 3 M soft) at 158 draw calls; Altdorf ground rendered uniform white at noon on 1 August (terrain-material/snow WIP); the new rigged characters read well but the frozen mid-stride poses in `altdorf-square-noon.png` suggest the exploration crowd is not yet driving `CharacterHandle.setSpeed` — once `WorldService.spawnCharacter` is stable, `npc.ts` `spawnMesh`/`syncMesh` should use it and call `setSpeed(actual m/s)` from `walkToward`.

## Explicitly out of reach for a browser engine / not exploration's to fix
- Crowd animation, distinct faces/clothing, the player rig: character art builder (`WorldService.spawnCharacter` / `CharacterHandle` landed in `services.ts`, exploration has not switched to it yet — expected once the assets exist).
- Terrain look, lake shores, sky, weather rendering, season foliage: world/art builders (`sky.ts`, `terrainMaterial.ts`, `water.ts` in flight).
- Navmesh pathing; ambient herds/boats; NPC chatter (rubric 10/10 items).
