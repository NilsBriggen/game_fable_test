# Bug hunt — quest & dialogue (src/quest, src/content/{quests,dialogues,cutscenes})

Probes: `tools/critic/probes/quest/{side-quest-start,duplicate-journal,xref}.test.ts` (run: `npx vitest run --config tools/critic/probes/quest/vitest.config.ts`), all passing — **as regression guards since the Phase 4 4.0 pass (2026-09-05)**: probes 1+2 originally proved each bug reproduced; after the fix they assert the fixed behaviour (quest starts; no duplicate line).

| # | Severity | File:line | Repro | Expected vs actual |
|---|----------|-----------|-------|---------------------|
| 1 | **blocker — FIXED 2026-09-05 (Phase 4 4.0)** | `src/content/dialogues/side.ts` giver nodes + first-stage ids in `src/content/quests/side/*` | Talk to any of the 6 side-quest giver NPCs and accept; probe: `side-quest-start.test.ts` (now a regression guard asserting start). | Was: opening dialogue only ever ran `{quest:['advance', qid, stage]}`, never `{quest:['start', qid]}`; `QuestMachine.advance()` silently no-op'd. Fix: accept choices now `{quest:['start', qid]}` and each quest's first stage id matches the stage the giver dialogue logically starts (`offer` for 4, `trouble` for fischer, `dispute`→`offer` alpstreit). All 6 start, journal, and tracker now. |
| 2 | **logic** | `src/content/quests/act1/der-eid.ts:30-33` and `der-hut.ts:50-53` | Lose `enc.brunnen-quay` (or `enc.hohle-gasse`) once, then win the retry; probe: `duplicate-journal.test.ts`. | Expected: at most one copy of the stage's journal line (cf. the Morgarten/muster-1315 retry in `src/quest/index.ts`'s `quest-failed` handler, which explicitly uses `{silentJournal:true}` + a distinct "second pass" line to avoid this). Actual: `escort-recover`/`hohle-gasse-recover`'s onEnter re-`advance`s straight back into `escort`/`hohle-gasse` via a plain effect (no `silentJournal`), so `QuestMachine.enterStage` (`quests.ts:118`) re-journals the identical stage text — confirmed duplicate `"A boat carries the elder toward Steinen..."` entries. |
| 3 | cosmetic | `src/quest/dialogue.ts:116-123,182-188` (`buildChoiceViews`) | A `DialogueNode.choices` array where every choice's `condition` evaluates false and none set `showDisabled` (no current content instance found — swept by `xref.test.ts`'s dead-end heuristic, 0 hits). | Expected: a node with choices always shows ≥1 pickable option or an unconditional `end`. Actual: `shown` can be `[]`; with a real UI, `ui.show()` is called with `choices:[]` — no escape, dialogue hangs. Not currently reachable in shipped content, but the runner has no guard against it, so a future content edit can soft-lock silently (validate() doesn't check this either). |

**Not bugs (checked, clean):** `ContentRegistry.validate()`-uncovered id references (quest/dialogue/cutscene/poi/npc/item/faction/skill ids in every `Effect`/`QuestCondition` across all quest/dialogue/cutscene content) — 0 dangling refs (`xref.test.ts`). Main-quest `advanceWhen` win/fled/lose coverage — all 4 encounter-gated stages (`der-eid`, `der-hut`, `marchenstreit`, `morgarten`) cover all three `CombatResult.outcome` values. `sceneDepth`/deferred-effects nesting, `setChapter` re-entrancy guard, skill-check DCs (max 16, all achievable), Tell companion add/remove around the ch1→ch2 chapter skip — all sound, already hardened by prior critic rounds (see `wave3-quest.md` comments throughout `src/quest/*`).
