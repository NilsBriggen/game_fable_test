# Bug hunt — party/save (round 3)

Scope: `src/party/*.ts`, `src/save/*.ts` (+ `src/core/ecs.ts` where save's load path exercises it), against
ARCHITECTURE §5.4/5.5/5.7. Rounds 1-2 (see `wave1-party.md`, `wave1-save.md`, in-code "Fix round N" comments)
already closed the XP-curve, item-id-collision, ammo/slot, party-cap, era-gating, rest/morale, invalidate-cache
and save-load-restore-order issues — not re-litigated here. Probes: `tools/critic/probes/party-save/round3.probe.test.ts`
(`npx vitest run --config tools/critic/probes/party-save/vitest.config.ts` → 3/3 pass, reproducing all 3 rows).

| # | File:line | Repro | Expected vs actual | Severity |
|---|---|---|---|---|
| 1 | `src/core/ecs.ts:220-226` (`World.clear()`), reached via `src/party`/`src/save`'s `resetWorld()` (`context.ts:91`) → `applyWorldState` (`snapshot.ts:135`) and `World.load()` (`ecs.ts:203-204`, calls `clear()` first) on every save load / new game | `w.destroy(id)` fires every `onRemove` listener for that entity's components; `w.clear()` (or `w.load(save)`, which calls `clear()` internally) wipes `stores`/`alive`/`tags` directly, calling none | Expected: teardown listeners (per §5.7 "modules tear down transient scene objects…") see every entity/component that disappears on load, symmetric with `destroy()`. Actual: `onRemove` never fires for entities alive before a `world.load()` — any future mesh/resource owner using `world.onRemove` (the documented mechanism `MeshRef`'s own comment implies) leaks silently on every load. Currently latent (nothing subscribes yet) but is exactly the load-time stale-entity/mesh gap. | Logic |
| 2 | `src/party/index.ts:469-473` (`equipInternal`, offHand two-handed guard) | Equip a `hands:2` bow into `ranged`, then equip any shield into `offHand` | Expected: a two-handed weapon (already enforced for `mainHand`, `index.ts:474-477`) should block a simultaneous off-hand shield the same way regardless of which slot holds it. Actual: the offHand guard reads only `eq.mainHand`'s weapon (`index.ts:470-472`); `eq.ranged` is never checked, so a two-handed bow/crossbow (`item.hunting-bow`, `item.armbrust`, …) and a shield equip at once — `derived()` then grants both the ranged attack and the shield's defense bonus simultaneously. | Logic |
| 3 | `src/save/db.ts:378-388` (`assertSaveShape`) | `assertSaveShape({schemaVersion:1, seed:1, gameTime:0, world:{entities:[{id:1}]}, rngState:{world:[]}})` passes; `new World().load(that.world)` then throws `Cannot convert undefined or null to object` | Expected: `importJson`'s only structural gate rejects a file that will fail to load, with a clear message, at import time. Actual: `assertSaveShape` checks only `schemaVersion`/`seed`/`gameTime`/`world.entities` (array)/`rngState.world` (array) — it never checks that each `world.entities[i]` carries a `components` object (or that `party`/`playerId`/`quests`/`flags`/`chapter`/`label` exist at all). Such a file is silently accepted by `importJson` and stored as a normal save slot; it only fails later, inside `doLoad()`'s try/catch, the next time someone tries to actually load that slot. | Logic |
