/** STUB — replaced by the party builder. Keeps the build green so the harness runs. */
import type { GameContext } from '@core/context';

export async function register(_ctx: GameContext): Promise<void> {
  console.warn('[stub] party module not implemented yet');
}
