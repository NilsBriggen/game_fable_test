/** STUB — replaced by the quest builder. Keeps the build green so the harness runs. */
import type { GameContext } from '@core/context';

export async function register(_ctx: GameContext): Promise<void> {
  console.warn('[stub] quest module not implemented yet');
}
