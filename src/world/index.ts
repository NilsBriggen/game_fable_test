/** STUB — replaced by the world builder. Keeps the build green so the harness runs. */
import type { GameContext } from '@core/context';

export async function register(_ctx: GameContext): Promise<void> {
  console.warn('[stub] world module not implemented yet');
}
